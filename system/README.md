# System

General scripts

- `upgrade.sh` -- update + upgrade + autoremove + clean
- `utils.sh` -- installs various utilities (vim,mc,nmon,htop, etc) and sets ll and .vimrc for both `root` and `$USERNAME`


